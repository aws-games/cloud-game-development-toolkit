# AWS Provider - optional, can rely on environment/credentials
# provider "aws" {
#   region = local.region
# }

# Kubernetes and Helm providers are configured within the DDC modules
# No provider configuration needed at the example level